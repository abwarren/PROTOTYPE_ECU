# Daily Engineering Log — 2026-07-01

## Repository: ECU Platform Core

---

### Sprint 005 — Compile the rusEFI fork successfully

**Goal:** Build toolchain verification and firmware compilation

### Completed

- **Git repository initialized** — Root repo at commit `361f3a4`; firmware upstream as git submodule
- **ARM GCC 12.3.Rel1 installed** — `/home/wa/tools/gcc-12/bin/` (satisfies >= 11.3.1 requirement)
- **Java 11.0.23 (Temurin) installed** — `/home/wa/tools/java/bin/`
- **7-Zip 23.01 installed** — `/home/wa/tools/7zip/7z` (needed for ramdisk image creation)
- **mtools 4.0.43 installed** — `/home/wa/tools/mtools/usr/bin/mcopy` (needed for DOS filesystem operations)
- **Firmware builds for f407-discovery** — Full build pipeline: Java code generation → C/C++ compilation → ramdisk creation → link → binary output
- **Build fix applied** — Added `-Wno-error=shadow` for GCC 12.x compatibility (stricter shadow checking)
- **START_HERE.md created** — Repository entry point with 8-document reading sequence
- **SESSION.md created** — Persistent session handoff document
- **D-009 resolved** — Build toolchain fully verified and documented

### Build Output

```
Target:         STM32F407 Discovery (f407-discovery)
Build host:     Linux x86_64
Toolchain:      ARM GCC 12.3.Rel1
Java:           OpenJDK 11.0.23 (Temurin)
Output files:   firmware/upstream/firmware/build/

File            Size        Description
rusefi.elf      26.5 MB     Executable with debug symbols
rusefi.bin      744 KB      Raw binary (flash image)
rusefi.hex      2.0 MB      Intel HEX format
rusefi.srec     2.2 MB      Motorola S-record format
rusefi_crc32.bin 744 KB     Binary with CRC32
```

### Build Environment Path

```bash
export PATH="/home/wa/tools/gcc-12/bin:/home/wa/tools/java/bin:/home/wa/tools/7zip:/home/wa/tools/mtools/usr/bin:/usr/sbin:$PATH"
export JAVA_HOME="/home/wa/tools/java"
```

### Problems Encountered and Solved

| Problem | Solution |
|---------|----------|
| Java not available | Downloaded Adoptium Temurin 11 JRE directly |
| apt-get requires sudo password | Downloaded all toolchain debs directly with `dpkg -x` |
| ARM GCC 10.3 too old (needs >= 11.3.1) | Upgraded to ARM GCC 12.3.Rel1 |
| `7z: command not found` at ramdisk stage | Downloaded 7-Zip 23.01 Linux CLI; symlinked `7zz` → `7z` |
| `mcopy: command not found` at image stage | Downloaded mtools 4.0.43 deb; extracted with `dpkg -x` |
| `-Werror=shadow` with GCC 12+ | Added `-Wno-error=shadow` to `USE_CPPOPT` in Makefile |
| `gcc_map_reader.jar` not found | Non-fatal — post-build script, no impact on binary output |

### Risks

| Risk | Likelihood | Impact | Status |
|------|------------|--------|--------|
| GPL license limits commercial distribution | High | Critical | 🔴 Active — module replacement roadmap in place |
| ARM GCC version mismatch on new machines | Medium | Medium | 🟡 Mitigated — documented toolchain version requirement |
| Build environment not reproducible | Low | Medium | 🟢 Mitigated — `scripts/build-firmware.sh` automates setup |

### Decisions

| ID | Title | Status |
|----|-------|--------|
| — | Use GCC 12.3 with `-Wno-error=shadow` rather than chase GCC 11.3 | Accepted |
| — | Install all tooling to `/home/wa/tools/` (user-local, no sudo) | Accepted |
| — | Dockerize build environment in Phase 2 for reproducibility | Deferred |

### Next

- Begin brand separation (replace rusEFI customer-facing strings with brand.json variables)
- Define firmware identity system (versioning, board IDs)
- Start Studio scaffold (Electron + React + TypeScript)
